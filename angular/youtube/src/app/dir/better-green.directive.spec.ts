import { BetterGreenDirective } from './better-green.directive';

describe('BetterGreenDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterGreenDirective();
    expect(directive).toBeTruthy();
  });
});
