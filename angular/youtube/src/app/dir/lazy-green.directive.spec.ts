import { LazyGreenDirective } from './lazy-green.directive';

describe('LazyGreenDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyGreenDirective();
    expect(directive).toBeTruthy();
  });
});
