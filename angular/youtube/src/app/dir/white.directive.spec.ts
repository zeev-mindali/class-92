import { WhiteDirective } from './white.directive';

describe('WhiteDirective', () => {
  it('should create an instance', () => {
    const directive = new WhiteDirective();
    expect(directive).toBeTruthy();
  });
});
