import { UserResultDirective } from './user-result.directive';

describe('UserResultDirective', () => {
  it('should create an instance', () => {
    const directive = new UserResultDirective();
    expect(directive).toBeTruthy();
  });
});
